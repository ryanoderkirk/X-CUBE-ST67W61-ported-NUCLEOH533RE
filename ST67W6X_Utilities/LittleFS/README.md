# Overview

Provided batch files convert the certificates and keys into a binary format that can be loaded into the Network Coprocessor.

> __IMPORTANT__
>
> The generated binary is required to load the NCP firmware on the ST67W611M Network Coprocessor.
